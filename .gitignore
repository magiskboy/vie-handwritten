# Python-generated files
__pycache__/
*.py[oc]
*.so
build/
dist/
wheels/
*.egg-info
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Virtual environments
.venv

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp

# Data (including charset) — keep local, not in git
/data/
# Archive nén của thư mục data (backup/chia sẻ ngoài git)
*.zip

# Artifacts
checkpoints/*
!checkpoints/.gitkeep
runs/
logs/
*.h5
*.keras
*.pb
*.onnx
*.tflite

# SpecStory / local agent
.specstory/

# Third-party build artifacts (KenLM built from submodule against system Boost)
/third_party/kenlm/build/

# Trained language model artifacts (rebuild with `build-lm`)
/lm/

# Local-only trees (kept on disk, not in git)
/demo/
/labs/


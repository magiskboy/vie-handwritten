# Python-generated files
__pycache__/
*.py[oc]
*.so
build/
dist/
wheels/
*.egg-info
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Virtual environments
.venv

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp

# Data: toàn bộ được nén thành data.zip và KHÔNG đẩy lên git.
# Ngoại lệ: charset (bảng ký tự) nhỏ và cần để chạy code nên vẫn track.
/data/*
!/data/charset/
# Archive nén của thư mục data (backup/chia sẻ ngoài git)
*.zip

# Artifacts
checkpoints/*
!checkpoints/.gitkeep
runs/
logs/
*.h5
*.keras
*.pb
*.onnx
*.tflite

# SpecStory / local agent
.specstory/

# Third-party build artifacts (KenLM built from submodule against system Boost)
/third_party/kenlm/build/

# Trained language model artifacts (rebuild with `build-lm`)
/lm/
